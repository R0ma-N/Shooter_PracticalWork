using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Shooter
{
    public class MainMenuController : MonoBehaviour
    {
        //private IOnInitialize[] _controllersInit;
        //private MenuMainCanvas _menuMainCanvas;
        //private ApplicationManager _applicationManager;

        //private void Awake()
        //{
        //    _menuMainCanvas = new MenuMainCanvas();
        //    _applicationManager = new ApplicationManager();

        //    _controllersInit = new IOnInitialize[1];
        //    _controllersInit[0] = _applicationManager;

        //    for (int i = 0; i < _controllersInit.Length; i++)
        //    {
        //        var controller = _controllersInit[i];
        //        controller.OnStart();
        //    }
        //}
    }
}
